#include <stdio.h>
#include <stdlib.h>
/**
 * We are given an integer array nums consisting of `n` elements, and an integer `k`
 * Find a contiguous subarray whose length is equal to `k` that has the maximum
 * average value and return this value. Any answer with a calculation error less than 10 ^-5 will be accepted.
 */
double  findMaxAverage(int *nums, int numsSize, int k)
{
    //constraint
    if (nums == numsSize)
    {

    }
    if( 1 <= k && k <= n && n <= 1e5)
    {

    }
    if(1e-4 <= nums[i] && nums[i] <= 1e4)
    {

    } 
}

int main(void)
{
    int input[] = [1,12,-5,-6,50,3];
    int k = 4;
    int numsSize = sizeof(input)/ sizeof(input[0]);
    findMaxAverage(input, numsSize, k);
}